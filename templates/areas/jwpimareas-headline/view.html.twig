{% set anchor = pimcore_input('anchor').getData() %}

{%- if headlineData is defined -%}
    <div{{ anchor ? ' id="' ~ anchor ~ '"' : '' }} class="jwpimareas-mt-{{ headlineData.hClass }}">
       {% include 'areas/jwpimareas-headline/headline.html.twig' %}
    </div>
{%- endif -%}
