{% extends '@PimAreaBricks/layout/layout.html.twig' %}
{% set infoId = info.getId | string %}

{%- block content -%}

    {{ pimcore_areablock("content_" ~ infoId, {
        'allowed': [
            'headline',
        ],
        'group': {
            ('areabrick.groups.layout' | trans({}, 'admin')): [
                'headline',
                'texteditor',
            ],
        },
    }) }}

{%- endblock -%}
